<%= form_with(model: member) do |form| %>
  <% if member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(member.errors.count, "error") %> prohibited this member from being saved:</h2>

      <ul>
        <% member.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
    <%#= form.label :'Member Privliges' %>
    <%#= form.select :isAdmin, ['Normal Member', 'Admin']%>
  <div class="field">
    <%= form.label :'Give admin priveleges?' %>
    <br>
    <%= form.select :isAdmin, [false, true]%>
    <%#= form.label :'Owner Privlige' %>
    <%#= form.select :isOwner, ['Non-owner', 'Owner'] %>
  </div>
  <div class="field">
    <%= form.label :'Give owner privileges?' %>
    <br>
    <%= form.select :isOwner, [false, true]%>
  </div>
  <div class="field">
    <%= form.label :'Member Points' %>
    <%= form.number_field(:totalPoints) %>
  </div>
  <br>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
